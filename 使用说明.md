# 微信公众号文章转PDF工具

## 功能介绍

这个工具可以将微信公众号文章内容转换为排版精美的PDF文档，解决以下问题：
- 微信文章链接无法直接分享或打开
- 长截图阅读体验差，无法搜索和复制文字
- 想要用AI分析文章内容但无法提取文字

## 支持功能

1. **文本输入模式**：直接粘贴文章文字内容，生成PDF
2. **OCR识别模式**：上传长截图，自动识别文字并生成PDF
3. **文件输入模式**：从文本文件读取文章内容

## 环境要求

### 基础依赖（必需）
```bash
pip install reportlab
```

### OCR功能（可选，用于图片识别）
```bash
pip install pytesseract pillow
```

同时需要安装Tesseract-OCR引擎：
- **Windows**: 下载安装包 https://github.com/UB-Mannheim/tesseract/wiki
- **Mac**: `brew install tesseract tesseract-lang`
- **Linux**: `sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim`

## 使用方法

### 方式一：从文本文件生成PDF

1. 将文章内容保存为文本文件（如 `article.txt`）
2. 运行命令：
```bash
python wechat_article_to_pdf.py --input article.txt --output article.pdf
```

### 方式二：从长截图OCR识别生成PDF

1. 准备公众号文章的长截图
2. 运行命令：
```bash
python wechat_article_to_pdf.py --image screenshot.png --output article.pdf
```

### 方式三：交互模式（直接粘贴文字）

```bash
python wechat_article_to_pdf.py --interactive
```

然后粘贴文章内容，按两次回车结束输入。

### 方式四：管道输入

```bash
cat article.txt | python wechat_article_to_pdf.py --output article.pdf
```

## 文章格式建议

为了获得最佳排版效果，建议文章文本按以下格式组织：

```
标题：文章标题

作者：作者名称

第一段内容...

第二段内容...

## 小标题

更多内容...

> 引用内容
```

工具会自动识别：
- 标题和作者信息
- 段落和小标题
- 引用块（以 `>` 开头）
- 粗体和斜体标记（`**粗体**`、`*斜体*`）

## 输出效果

生成的PDF包含：
- 专业的排版样式（微信风格）
- 清晰的标题层次
- 舒适的阅读体验
- 页脚信息（生成日期）

## 示例

```bash
# 示例1：基本使用
python wechat_article_to_pdf.py --input article.txt

# 示例2：指定标题
python wechat_article_to_pdf.py --input article.txt --title "我的文章"

# 示例3：从图片识别
python wechat_article_to_pdf.py --image wechat_screenshot.png --output output.pdf
```

## 常见问题

**Q: 生成的PDF中文显示乱码？**
A: 工具会自动检测系统字体。如果显示乱码，请确保系统安装了中文字体（如微软雅黑、宋体等）。

**Q: OCR识别不准确？**
A: 请确保截图清晰，文字对比度高。可以尝试调整图片大小或裁剪掉无关区域。

**Q: 如何批量处理多篇文章？**
A: 可以编写简单的批处理脚本，循环调用本工具处理多个文件。

## 技术说明

- 使用 `reportlab` 库生成PDF
- 支持CJK字体（中文、日文、韩文）
- 自动识别文章结构（标题、段落、引用等）
- 基于Tesseract的OCR文字识别
