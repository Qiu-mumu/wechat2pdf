# 微信公众号文章转PDF - 网页版

## 功能介绍

手机网页版工具，无需安装任何App，浏览器打开即可使用！

### 特点
- 📱 **手机直接使用**：浏览器打开即用
- 📝 **粘贴文字**：直接粘贴文章内容
- 📷 **上传截图**：自动OCR识别文字
- 📄 **生成PDF**：排版精美，可搜索可复制

## 安装和运行

### 1. 安装依赖

```bash
pip install flask reportlab
```

如果需要OCR功能（上传截图识别）：
```bash
pip install pytesseract pillow
```

同时安装Tesseract-OCR引擎：
- **Windows**: 下载 https://github.com/UB-Mannheim/tesseract/wiki
- **Mac**: `brew install tesseract`
- **Linux**: `sudo apt-get install tesseract-ocr`

### 2. 运行服务

```bash
python app.py
```

服务启动后，会显示访问地址：
- 本机访问：http://localhost:5000
- 局域网访问：http://你的IP:5000

### 3. 手机访问

**方式一：同一WiFi下访问**
1. 确保手机和电脑连接同一个WiFi
2. 手机浏览器输入电脑的IP地址（如 http://192.168.1.100:5000）

**方式二：本机测试**
1. 电脑浏览器访问 http://localhost:5000
2. 按F12打开开发者工具，切换到手机模式预览

## 使用方法

### 方式一：粘贴文字
1. 在手机上复制微信公众号文章内容
2. 打开网页，选择"粘贴文字"标签
3. 粘贴文章内容到输入框
4. 点击"生成PDF文档"
5. 下载生成的PDF文件

### 方式二：上传截图
1. 在手机上截取公众号文章长截图
2. 打开网页，选择"上传截图"标签
3. 点击上传区域，选择截图
4. 点击"生成PDF文档"
5. 等待OCR识别和PDF生成
6. 下载生成的PDF文件

## 项目文件说明

```
项目目录/
├── app.py              # 后端服务（Flask）
├── templates/
│   └── index.html      # 前端页面
└── 网页版使用说明.md    # 本说明文件
```

## 常见问题

**Q: 手机无法访问电脑服务？**
A: 确保手机和电脑在同一WiFi网络，并检查防火墙设置。

**Q: OCR识别不准确？**
A: 请确保截图清晰，文字对比度高。可以裁剪掉无关区域。

**Q: 如何在外网访问？**
A: 可以使用内网穿透工具（如ngrok）或部署到云服务器。

## 部署到云服务器（进阶）

如果想让任何人都能使用，可以部署到云服务器：

1. 购买云服务器（阿里云、腾讯云等）
2. 安装Python和依赖
3. 上传项目文件
4. 使用gunicorn运行：
   ```bash
   pip install gunicorn
   gunicorn -w 4 -b 0.0.0.0:80 app:app
   ```
5. 配置域名和HTTPS

## 技术栈

- **前端**：HTML5 + CSS3 + JavaScript（响应式设计）
- **后端**：Python Flask
- **PDF生成**：ReportLab
- **OCR识别**：Tesseract + pytesseract
